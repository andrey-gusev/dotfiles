general {
    font_family = Noto Sans
    hide_cursor = true
}

background {
    color = black
}

label {
    text = $TIME
    color = white
    font_size = 100
    position = 0, 80
    halign = center
    valign = center
}

input-field {
    size = 50, 50
    outline_thickness = 10
    fade_on_empty = true
    hide_input = true
    placeholder_text = 
    position = 0, -50
    halign = center
    valign = center
}

label {
    text = $LAYOUT
    color = white
    font_size = 14
    position = -10, 10
    halign = right
    valign = bottom
}
